{
  "name": "doudou-chat-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "electron/main.js",
  "scripts": {
    "electron:dev": "electron .",
    "sync:version": "node scripts/sync-version.cjs",
    "electron:build": "pnpm run sync:version && pnpm -C front build && electron-builder",
    "electron:pack": "pnpm run sync:version && pnpm -C front build && electron-builder --dir"
  },
  "dependencies": {
    "tree-kill": "^1.2.2"
  },
  "devDependencies": {
    "electron": "^32.0.0",
    "electron-builder": "^24.13.3"
  }
}
